main function
{
	SetTimer();
	userApp();
	while (timer is not flagged) { loop }
}

userApp()
{
	Increment LED Number
	Update
}

SetTimer()
{
	Turn it off;
	Set integer where we want timer to start so it rolls over at the correct amount of time;
	Set TMR0H and TMR0H using the integer just set;
	turn off flag;
	Turn on timer;
}